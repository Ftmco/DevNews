export type ChannelModel = {
    link: string;
    title: string;
    avatar: string;
    name: string;
    token: string;
    id: string;
}

export type SaveFile = {
    base64: string;
    path: string;
}